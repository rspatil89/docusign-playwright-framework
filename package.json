{
  "name": "docusign-playwright",
  "version": "1.0.0",
  "scripts": {
    "test": "npx playwright test",
    "report": "npx allure generate ./allure-results --clean -o ./allure-report && npx allure open ./allure-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "dotenv": "^16.3.1",
    "typescript": "^5.2.0",
    "allure-playwright": "^2.0.0"
  }
}
